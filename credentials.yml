rest:
  # No additional configuration needed for the REST channel.
  # Your FastAPI orchestrator will POST to:
  # http://localhost:5005/webhooks/rest/webhook
  # with a body like: {"sender": "<session_id>", "message": "<user text>"}
