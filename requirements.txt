

fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
motor==3.5.1        # async MongoDB driver
pymongo==4.8.0
pydantic==2.8.2


Microphone → Web Speech API (STT) → FastAPI → Rasa
Rasa response → FastAPI → Web Speech API (TTS) → Speaker
After each exchange → IQ rating slider (1-5) → FastAPI → MongoDB